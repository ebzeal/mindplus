import React from 'react';

import InputDiv from './input.styles';

const Input = ({ type, size, placeholder }) => <InputDiv type={type} size={size} placeholder={placeholder} />;

export default Input;
