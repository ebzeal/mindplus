import styled from 'styled-components';

const ContactDiv = styled.div`
  display: flex;
  flex-wrap: wrap;
  margin-top: 50px;
  width: 100%;

  input {
    flex-basis: 60%;
  }

  button {
    flex-basis: 40%;
  }
`;

export default ContactDiv;
